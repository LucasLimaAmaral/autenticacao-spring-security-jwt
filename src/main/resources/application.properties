spring.application.name=token-jwt

#configura��o banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/meu_sistema?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Dialeto do MySQL para Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Atualizar automaticamente o banco ao iniciar a aplica��o
spring.jpa.hibernate.ddl-auto=update

# Mostrar as queries no console
spring.jpa.show-sql=true

api.security.token.secret=${JWT_SECRET:my-secret-key}
